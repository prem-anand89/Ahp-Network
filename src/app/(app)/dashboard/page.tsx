// Placeholder — Phase 1 builds the real authenticated dashboard. This page
// exists so the (app) route group has a route to render during the CI
// static/dynamic assertion.

export default function DashboardPlaceholder() {
  return <main className="p-6">Dashboard — Phase 1</main>;
}
