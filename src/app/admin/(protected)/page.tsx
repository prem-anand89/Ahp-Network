// Placeholder — the real admin IA (verification, practice claims,
// communities, referral ops, grievance, feedback — §8G6) is a later phase.
// This exists so /admin has a route to render and the gating layers above
// have something to protect.

export default function AdminHomePlaceholder() {
  return <main className="p-6">Admin — Phase 1 placeholder</main>;
}
