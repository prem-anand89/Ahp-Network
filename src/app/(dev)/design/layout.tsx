export const metadata = { title: "Design system — internal", robots: { index: false, follow: false } };

export default function DesignLayout({ children }: { children: React.ReactNode }) {
  return children;
}
